# MongoDB connection (example local)
spring.data.mongodb.uri=mongodb://localhost:27017/smartsafety

# Server port
server.port=8080

# App flags
app.data.load=true

# JWT (demo)
jwt.secret=change-me-please
jwt.expiration-ms=86400000

# Logging
logging.level.org.springframework=INFO
logging.level.com.smartsafety=DEBUG
